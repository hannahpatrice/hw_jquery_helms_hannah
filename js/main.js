$(document).ready(function () {
    $('img').remove();

    var paragraphs = $('p');

    paragraphs.css({
        'color': 'blue',
        'font-size': '10px',
        'text-decoration': 'line-through',
    });
});
